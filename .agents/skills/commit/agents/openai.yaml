interface:
  display_name: "commit"
  short_description: "Group changes and create focused commits."
  default_prompt: "Use $commit to inspect my changes and commit the relevant work in logical groups."
